{
  "loop": {
    "mcp__echo__echo": {
      "name": "mcp__echo__echo",
      "description": "Echo a phrase back.",
      "input_schema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "The phrase to echo."
          }
        },
        "required": [
          "text"
        ]
      },
      "side_effect": "none"
    },
    "mcp__echo__write_note": {
      "name": "mcp__echo__write_note",
      "description": "Append a line to the shared note file.",
      "input_schema": {
        "type": "object",
        "properties": {
          "line": {
            "type": "string"
          }
        },
        "required": [
          "line"
        ]
      },
      "side_effect": "external_effect"
    }
  },
  "loop2": {
    "mcp__echo__echo": {
      "name": "mcp__echo__echo",
      "description": "Echo a phrase back.",
      "input_schema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "The phrase to echo."
          }
        },
        "required": [
          "text"
        ]
      }
    },
    "mcp__echo__write_note": {
      "name": "mcp__echo__write_note",
      "description": "Append a line to the shared note file.",
      "input_schema": {
        "type": "object",
        "properties": {
          "line": {
            "type": "string"
          }
        },
        "required": [
          "line"
        ]
      }
    }
  }
}
