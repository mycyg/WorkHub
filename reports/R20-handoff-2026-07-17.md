# R20 修复流水线 Handoff(2026-07-17)

负责人流水线模式收尾快照。给下一个 session 接手用。

## 一、已落 main 的内容(三波 PR,全部 CI 8/8 绿)

基线起点 main `33f52b16`(R19 报告已并),终点 main `6cce2225`。

| PR | 内容 | merge commit |
|---|---|---|
| #11 | R19 报告+SSOT+合流计划(doc-only,含 codex 交叉反馈修订) | d46676cc |
| #12 | **Phase 0 安全封口 + Phase 1 可靠性**(2 P0 + 4 P1) | dbed8e58 |
| #13 | wave2:Phase 1 收尾 + Phase 2A API 打底 | a19eff91 |
| #14 | wave3:Phase 2 断链(web/桌面/壳)+ loop2 on-parity + 对话轮默认 on | 6cce2225 |

### 已修缺陷逐条(codex 编号 / R19 编号)

**Phase 0 安全(codex HOLD 解除门槛,全部真实身份轮换/并发集成测试佐证):**
- P0-01/P1-09/P2-03(SEC-1):成员移出 fail-closed(resolveHumanActor 无 active 成员行即 403 workspace_access_revoked、不再回退默认租户;identify 拒复活软删墓碑;移出=同事务撤销 session/device token + 提交后清 presence;最后管理员 TOCTOU 用 workspace 级 pg advisory 锁;审计失败结构化告警不静默吞)。
- P0-02/P1-01(SEC-2):Rust 身份代际 generation(set/clear 都递增+notify)、pump select! 按代际中止旧身份 SSE、服务端 SSE 心跳重验 grant(撤销写 stream.revoked 收流)、桌面 logout 有序事务(失败可见可重试+force 兜底)。

**Phase 1 可靠性:**
- P1-10(REL-1):queued AgentRun 启动 drain,不再以 requeued>0 为闸 + 启动即 tick。
- P1-11(REL-2,迁移 0068 additive):ProactiveIntent created 可恢复态(同 key 恢复投递 + pulse 扫描 attempt 封顶 3 判 stalled)。
- P1-12(REL-3):依赖 DAG 加边前项目级 pg_advisory_xact_lock(真 PG 并发复现佐证)。
- P1-03(REL-4):桌面 CU run SSE/poll 可恢复状态机(EOF/异常退避重连+轮询不永久停+重连终态对账)。
- P1-02(REL-5):password/hybrid 模式桌面凭据登录链路(登录建会话→desktop-bootstrap exchange 设备 token)。
- P1-04(REL-6):第二实例深链 create-if-missing(与正常深链共享 handle_deep_link_plan)。

**Phase 2A API 打底(全 additive):**
- P1-08(P2A-roster):GET /api/workspace/roster(workspace 隔离+分页不截断 200+capability)。**web/桌面消费端已切**(见 2B/2C),但注意 web 审批「转交同事」选择器仍用全局 /api/users(依赖 roster 不含的 is_admin 标签,WEB-1 精确范围保留)。
- R19-19/18/22/21(P2A-project-ops):项目归档/删除、工作项指派/认领、评论、工作区审计列表端点。

**Phase 2B/2C/2D 断链:**
- WEB-1:P1-05 邀请全链(token 不被抹+/invite 接受落地页+DELETE 撤销路由)、P1-06 Settings 常驻、P1-07 部分失败诚实化、P1-08 web 半边 roster 切换、P2-08 二次确认。
- DSK-UX:R19-5 策略撤销 UI、R19-23 网盘删除确认+回收站、R19-25 键盘可达、R19-11 presence 单源、R19-9 未读重连补拉、R19-40 新 DM 即时、R19-3 AI 改动回滚(SDK+共享 replay 按钮+桌面 live 壳挂载)。
- DSK-NATIVE:P2-07/R19-12 通知点击深链、R19-16 Dock Reopen、R19-13 壳层 locale 运行时同步。

**loop2:**
- abort-during-tool 挂死修复(loop2 未知工具不委派 execute 注册表的真实语义缺口,opt-in resolveMissingTool 钩子)。
- 对话轮 usage/elapsed 戳失修复。
- **CONVERSATION_TURN_LOOP2_MODE 默认翻 on**;**AGENT_RUN_LOOP2_MODE 仍 off**(见遗留)。

## 二、遗留未做(下一个 session 的工作面,按 SSOT 02-ssot-2026-07-17.md)

### Phase 2 剩余(前端呈现,后端已就绪)
- **P1-08 收尾**:web 审批转交选择器仍用 /api/users,roster 端点缺 is_admin 标签——要么给 roster 加标签、要么该选择器另接。
- **P2-05 设备管理 UI**:client-devices 端点+SDK 齐全,零 UI。落个人 Settings「已登录设备」。
- **P2-06 web 会话镜像实时**:web 不订阅 conversation SSE 却宣称自动刷新;typed client 已有窄流 URL,要订阅+按 seq 合并+重连补拉。
- **R19-15 web 个人空间/私聊导航入口**。
- **R19-6 成本页三维度**:by_workitem/trend/by_team 后端 admin 填了 web 不渲(桌面还把用户指过去)。
- **R19-1/2 OKR/预算策略 UI**:端点在前端不可达。
- **R19-3 UI 已完成**(本波做完),但真机验收待用户。

### Phase 3(一致性/工程质量/打磨)
- P2-01 消息 outbox/replay、P2-02 offboarding 可重入、P2-04 会话 rename 领域事件、P2-09 语言偏好同步提示、P2-10 头像弹窗焦点生命周期、P2-11 Chrome QA 日志。
- P3-01 cargo fmt/clippy 进 CI 并清零(注:deep_link.rs/windows.rs 有既存 fmt 漂移,多个 worker 报过)。
- P3-02 预算续租可观测。
- R19-7/34-37 数据层孤儿表处置(user_profiles 已从死表清单剔除)、R19-29/30 死端点/死参数。

### loop2 AGENT_RUN schema 翻转(独立工程)
- abort parity 已修,但约 40 个用 `settings: runtimeSettings` 的 agent-run 单测断言 loop.ts legacy 行为。schema 默认翻 on 前需专门迁移那批(逐条判断钉 off 还是按 on 重验)。生产当前可经部署 env `AGENT_RUN_LOOP2_MODE=on` 覆盖启用(引擎等价性由 shadow-assert 守)。

### 待产品拍板(不盲建,来自 SSOT)
- R19-44 通知偏好粒度、R19-45 用户 reactivate、R19-46 项目级成员/访问控制、R19-33 Cuu 主动性三档接频控、R19-28 合并融合 preview 步。

## 三、真机(.app)验收清单(仅用户能做,浏览器 pane 渲不出 Tauri)
- 全局 Option+Space 热键、Dock 角标、跨窗登出、DM 端到端、锁屏 OS 通知。
- **本波新增**:通知点击深链(注意 DSK-NATIVE 标注:tauri 插件拿不到 OS 弹窗点击信号,现实现是 webview 收到计划即经命令桥执行,请核实是否会在非点击场景过早前置主窗)、Dock Reopen、壳层 locale 切换即时生效+托盘菜单重建、网盘删除确认+回收站、AI 改动回滚按钮、桌面密码模式登录。

## 四、仓库/环境状态
- **main = 6cce2225**,含 R20 全部三波。所有 r20/* 分支已并,worktree 仍在 `~/Desktop/开发项目/wh-r20/*`(19 个,可清理:`git worktree prune` + 手动删目录)。
- 迁移号:0068 已用(REL-2),下一个空闲 = 0069。
- 审查只读检出 `/private/tmp/wh-r19-review`(钉 33f52b16)可留作对照或删。
- SSOT 唯一事实源:`r19-iteration-review/02-ssot-2026-07-17.md`(68 个独立问题,已标记处置状态)。

## 五、流水线纪律备忘(下个 session 沿用)
- 工人只用 opus/sonnet,绝不 fable 全量 fan-out。
- 隔离 worktree 单写者;负责人逐单审查 diff + 独立复验(根因测试修复前红/后绿 + typecheck + 范围纪律)+ 集成终验后才 PR。
- 绝不 git add -A(只 targeted add);工人禁跑 qa/smoke 全家桶、禁重生成 artifacts、禁自行加迁移(抢号)。
- CI 逐 job 核 conclusion(gh run view --json jobs),别信 watch 退出码;网络抖动用重试循环。
- **CI 门坑**:改 Rust 系统字符串/locale 要同步 scripts/qa/r4-rust-system-i18n.ts 的源码字符串门;web 增删路由同步 routeTreeRouteCount(当前 22,接受页不进 SPA 表规避);cargo fmt 会连带重排域外文件需 git checkout 还原;workspace job 的 launchChrome 测试 CI 计时敏感偶发 flake,复跑即绿。
