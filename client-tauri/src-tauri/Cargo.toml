[package]
name = "workhub-client-tauri"
version = "0.1.0"
edition = "2021"

[lib]
name = "workhub_client_tauri"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
