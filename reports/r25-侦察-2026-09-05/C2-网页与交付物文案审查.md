# A2 文案审查：网页端 / 共享 UI / 服务端页面 VM / 错误与通知 / 交付物模板

**一句话结论：** 96 处违规，16 高 74 中 6 低——问题不是零散错字，而是四条系统性裂缝：界面在向用户解说自己的设计（右栏常驻「数据源 / 实时数据」、每个路由的加载/错误/403 态正文是规格原文）、内部实现词与裸 id 从服务端一路透到界面（`ok`、`field_merge`、`proposal.merge audit`、用户 uuid 当人名）、英文文案完全绕开了禁词门（`option-first` 在门禁扫描的同一个文件里存活）、以及 AI 融合交付物把合并过程当成了交付内容。

---

## 一、网页路由（apps/web/src）

| # | 位置 | 原文 | 分类 | 严重度 | 建议 | 为什么 |
|---|---|---|---|---|---|---|
| A2-54 | `apps/web/src/browser.ts:2730,2768` | 「已物化」／「查看详情、审批与物化在桌面客户端的日程标签进行。」 | B | 中 | 「已生成任务」／「查看详情、审批和生成任务在桌面客户端的日程标签里进行。」 | 「物化」是 materialize 的数据库直译，当状态徽章直出；全仓 19 处 |
| A2-55 | `apps/web/src/browser.ts:3267` | 「失焦自动保存，留空则不注入项目级指令。」 | B | 中 | 「离开输入框会自动保存；留空表示这个项目不加额外指令。」 | 失焦=blur 事件名、注入=prompt 拼装动词 |
| A2-56 | `apps/web/src/browser.ts:950` | 「已转交，这条审批会路由给对方处理。」 | B | 中 | 「已转交，这条审批现在归对方处理。」 | 「路由」是分派实现词；第 947 行同一动作已有正确写法，两条并存 |
| A2-57 | `apps/web/src/auth-screen-mode.ts:65` | 「当前后端未启用这种登录方式，请刷新页面重试。」 | B | 中 | 「这个团队没有开启这种登录方式，请刷新页面重试。」 | 「后端」出现在登录屏——用户见到的第一屏 |
| A2-58 | `apps/web/src/browser.ts:4558,4633` | 「邀请功能需要密码登录模式（当前工作区为昵称模式，无需邀请即可加入）。」 | B | 中 | 「这个工作区用昵称直接加入，不需要邀请。」 | 「密码登录模式／昵称模式」是内部部署枚举 |
| A2-60 | `apps/web/src/objective-panel.ts:31,141,145`（web 侧共 18 处） | 「挂链／挂链工作项／挂链中…」 | B | 中 | 「关联／关联的任务／关联中…」 | 「挂链」是内部口头语；同概念在 `route-components.ts:4852` 已写作「挂上去」 |
| A2-59 | `apps/web/src/drive-preview.ts:37` | 「缓存文本预览」 | B | 低 | 「文本预览」 | 「缓存」是取数来源实现细节 |
| A2-61 | `apps/web/src/routes.ts:698` | 「自治率」 | B | 低 | 「自主率」 | 同一指标在 `route-components.ts:793/1622` 叫「自主率」 |
| A2-62 | `apps/web/src/routes.ts:1767` | `surfaceLabel: "Web R4"` | B | 低 | 删除该参数 | 内部轮次代号；当前渲染路径已死，但留着就是一次接线之隔 |

---

## 二、共享 UI 字典（packages/ui/src）— 密度最高

### 2.1 界面在解说自己（高）

| # | 位置 | 原文 | 严重度 | 建议 |
|---|---|---|---|---|
| A2-01 | `packages/ui/src/route-state.ts:174` | en：`Loading real data` / `The page waits for the backend service instead of showing fake success.` | 高 | `Loading` / `Fetching the latest from the server.` |
| A2-02 | `packages/ui/src/route-state.ts:180` | en：`Empty states stay quiet and leave a create, back, or history entry.` | 高 | `Nothing here yet. Create something new, or head back to the overview.` |
| A2-03 | `packages/ui/src/route-state.ts:185` | en：`The page keeps context and a trace id so users can retry and engineers can debug.` | 高 | `The error is recorded. Try again — if it keeps failing, send this page to your admin.` |
| A2-04 | `packages/ui/src/route-state.ts:190` | en：`The state explains who can grant access without exposing private work.` | 高 | `This area needs permission. Ask someone with access to grant it.` |
| A2-05 | `packages/ui/src/route-state.ts:147` | 「正在加载真实数据」／「正在等待后台返回真实数据，不会显示假的成功或过期内容。」 | 高 | 「正在加载」／「正在读取最新数据，稍等一下。」 |
| A2-07 | `packages/ui/src/gold-path/product-shell.ts:630-632` | 「数据源 / 实时数据 / 有新动态会自动刷新，数据以后台为准。」（en：`Data source / Live data / the backend stays the source of truth`） | 高 | 整块删除；如需保留只留「有新动态会自动刷新」一行放顶栏状态点旁 |

这四条 en 状态文案是设计规格原文（「空态该怎么设计」「工程师可以据此调试」），经 `apps/web/src/routes.ts:1710` 渲染在**每一个** web 路由的加载/错误/403/404 态上。A2-07 的右栏区块在**每一页**常驻，本质是向评审证明「不是假数据」的开发期声明。

其余同类：A2-16/17/18（设置页三条把分层决策与 `shared contracts`、`client layer` 写给用户）、A2-21、A2-29、A2-34（「项目主页摘要展示…」「本页」「此页」）、A2-96（「本页只加载了前 N 个」）、A2-12（`masthead.*` 十七条「X 页汇总 Y」，含「项目即产品」「像仓库索引」「像代码评审 / read like PRs」；当前未被渲染，属已入库死文案，建议删除而非留着）。

### 2.2 英文侧全线漏检（高）

| # | 位置 | 原文 | 严重度 | 建议 |
|---|---|---|---|---|
| A2-14 | `packages/ui/src/gold-path/i18n.ts:644` | `Intake stays option-first, so Web will not submit an empty clarification.` | 高 | `Pick one of the options to continue.` |
| A2-15 | `packages/ui/src/gold-path/route-components.ts:923` | `WorkHub prepares the pilot project context first, then opens option-first intake.` | 高 | `Pick a direction and AI starts from there. Accepted deliverables stay untouched until you approve a change.` |
| A2-20 | `packages/ui/src/gold-path/i18n.ts:470` | `AI-first home`（zh 为「今日焦点」） | 中 | `Today's focus` |
| A2-22 | `packages/ui/src/gold-path/i18n.ts:526` | `AI trace preview`（zh 已是「AI 工作过程」） | 中 | `How AI worked on this` |
| A2-24 | `packages/ui/src/i18n.ts:136`、`gold-path/i18n.ts:571-573`、`replay/render.ts:112` | `Current hunks / Incoming hunks / Folded hunks / Overlap hunk` | 中 | `Changes in the current version / Changes in this version / Collapsed sections / Overlapping section` |
| A2-25 | `packages/ui/src/gold-path/i18n.ts:642,631` | `Web will not submit an empty advanced edit.` / `so Web keeps only the entry point and status` | 中 | `an empty field won't be submitted` ／ 去掉后半句 |
| A2-11 | `packages/ui/src/gold-path/product-shell.ts:297` | `Trace` | 中 | `Steps` |

`option-first` 与 `trace` **都在 `scripts/dev/check-copy-terms.ts` 的禁词表里**，`route-components.ts` 也**在**门禁的扫描清单里——它们仍然存活，因为门禁第 60 行 `if (!/[一-鿿]/.test(line)) return;` 跳过所有不含中文的行，而 gold-path 词典的 zh/en 是分块写的。这是本轮最值得优先修的机制性缺口（见末节）。

### 2.3 内部枚举 / 裸 id 直出（高+中）

| # | 位置 | 原文 | 严重度 | 建议 |
|---|---|---|---|---|
| A2-13 | `packages/ui/src/gold-path/i18n.ts:391,588` | `"cost.statusFallback": "ok"` | 高 | 「正常」/`OK`（同仓 `packages/ui/src/i18n.ts:321` 已有 `budgetStatusLabels` 未被复用） |
| A2-31 | `packages/ui/src/gold-path/route-components.ts:5392,5396` | 「用量统计还没接入」「下面的数字暂时都是 0，因为用量统计还没接好；接入后这里会显示真实成本。」 | 高 | 「还没有用量记录」「这个工作区还没有产生 AI 花费。派一个任务给 AI，成本会出现在这里。」 |
| A2-47 | `packages/ui/src/structured-field-details.ts:99,102,179` | 「策略: accept_incoming」「类型: object」「策略: field_merge」 | 中 | 「处理方式：采纳这次版本」／删除类型片／「处理方式：按字段合并」 |
| A2-48 | `packages/ui/src/structured-field-details.ts:171` | 「快照: 8f2a13cd」 | 中 | 删除，或改「可还原到改动前」 |
| A2-49 | `packages/ui/src/replay/render.ts:221` | 「结果校验码 · 3f7a91c25d81…」（title 挂全 sha256） | 中 | 删除 |
| A2-50 | `packages/ui/src/proposal/render.ts:592-597` | 「正式版: 3f7a91c25d」「这次版本: 8b1e04af92」 | 中 | 改为版本时间/版本号 |
| A2-36 | `packages/ui/src/gold-path/route-components.ts:2446` | 「事项 3f7a91c2」 | 中 | 用实体标题，取不到就只显类型 |
| A2-37 | `packages/ui/src/gold-path/route-components.ts:5276`（另 5166/5228/4980/5050） | 团队/任务计划/目标/依赖行只显 uuid 前 8 位 | 中 | 用名称，取不到就省略该行 |
| A2-35 | `packages/ui/src/gold-path/route-components.ts:2994` | 「跨 run 审计时间线」 | 中 | 「这个任务的全部改动记录」 |
| A2-95 | `packages/ui/src/i18n.ts:512`（+505-508） | `labelFromMap` 未命中回落 `uiHumanize`，把未映射枚举摊平直出 | 中 | 回落到中性词并在开发环境告警 |

A2-50 尤其刺眼：冲突卡正文写着「不用看技术细节，直接选一个处理方式」（`packages/ui/src/i18n.ts:90`），旁边就挂着两串 sha256。

### 2.4 内部代号与两套话（中）

| # | 位置 | 原文 | 建议 | 备注 |
|---|---|---|---|---|
| A2-10 | `product-shell.ts:144` | 「军团」 | 「AI 小组」/`AI teams` | 同一页三个名：导航「军团」、`gold-path/render.ts:152`「智能代理军团」、`route-components.ts:1708/5200`「指挥台」；zh 侧 ui 39 / web 5 / api 16 处 |
| A2-53 | `gold-path/render.ts:136` | 「AI 优先首页 / 选项接入 / 智能代理军团 / 成本看板」 | 「总览 / 新任务 / AI 小组 / 成本」 | 与 `product-shell.ts:137-160` 的导航标签逐条不同 |
| A2-32 | `route-components.ts:627`（+625/628/632/1809/1811/1860/1862） | 「WorkHub 会先准备好试点项目，再进入选项优先的需求澄清」 | 「先给你几个选项把任务问清楚；在你确认前，不会改动已采纳的交付物。」 | 「选项优先」= option-first 直译；「试点项目」被用作真实默认项目名 |
| A2-33 | `route-components.ts:1866`（+`routes.ts:986`、`product-shell.ts:155/248`） | 「实时数据」徽章 | 删除 | 开发期非假数据标记，对用户零信息 |
| A2-23 | `gold-path/i18n.ts:303`（+`route-components.ts:1390`、`gold-path/render.ts:122`） | 「未路由 / 已路由」 | 「还没指派审批人 / 已指派审批人」 | |
| A2-26 | `packages/ui/src/i18n.ts:234` | 「PM 模式处理中」 | 「AI 项目经理接手中」 | 内部运行模式代号当任务状态徽章 |
| A2-42 | `route-components.ts:5363`（+`agent-runner.ts:1516`） | 「全托管模式（第 5 档）」 | 「在「全托管」下…」 | 内部档位编号 |
| A2-08/A2-28 | `product-shell.ts:165`、`packages/ui/src/i18n.ts:82` | 「回灌给 AI」 | 「AI 会照着你的理由继续改」 | |
| A2-44 | `route-components.ts:1605,1773` | 「每个项目像一个仓库」/`behaves like a repo`、「避免把文件散在全局入口」 | 「一个项目里装着它的任务、文件、版本和网盘。」 | 违反 glossary §1.2 第 1 条 |
| A2-27 | `packages/ui/src/i18n.ts:286` | 「二进制文档」 | 「其他文件」 | |
| A2-30 | `packages/ui/src/i18n.ts:106` | 「默认仍是一件事优先」 | 「先处理最要紧的一件」 | 「仍是」暗示存在被改掉的旧版；D 类 |
| A2-19 | `gold-path/i18n.ts:405,602` | 「部分动态内容仍在逐步支持多语言」/`will keep moving onto locale-aware payloads` | 「顶部可以切换界面语言。」 | 路线图 + 数据结构名，B+D |

### 2.5 运维口径流入用户面（中）

A2-38（`route-components.ts:6088`「这里是这台服务器上装好的插件（兼容 DeepSeek Harness 的工具类插件）」——含内部参考项目代号）、A2-39（`6131/6135`「安全边界，非故障」「「是」为正常态」——写给 QA 的注解）、A2-40（`6176`「密钥只存服务端环境变量」）、A2-41（`836/840`「这台服务器」「服务器这次启动后」）、A2-45（`857-870` 工作进程数/消息服务/数据库/运行时，应收进同页 6160 行已有的「系统诊断（管理员）」区）、A2-46（`5995` 邀请「令牌」+「服务端不再可取回」）、A2-51（`onboarding.ts:176,189,38`「这个实例」「本实例」「局域网信任模式」——登录第一屏）、A2-52（`invite-accept.ts:40`「服务端只存哈希」）、A2-43（`5348`「而不是按提任务的人」记账口径辩护）。

---

## 三、服务端页面视图模型（apps/api/src/pages）

| # | 位置 | 原文 | 分类 | 严重度 | 建议 | 为什么 |
|---|---|---|---|---|---|---|
| A2-63 | `apps/api/src/pages/cost.ts:183` | `label: item.id`（他人行 = 完整用户 uuid） | B | 高 | 用成员昵称；取不到用「已停用成员」 | 成本页「按人花费」除自己外每行标签都是 uuid，管理员看不出是谁 |
| A2-65 | `apps/api/src/pages/replay.ts:255` | `` title: `${log.action} audit` ``→「proposal.merge audit」 | B | 高 | 按动作查表出人话标题 | 回放页每条证据标题是内部枚举 + 英文 audit，不随语言变化 |
| A2-64 | `apps/api/src/pages/cost.ts:196` | `code: item.id` | B | 中 | 用工作项 code 与标题 | 内部 id 被当业务编号渲染 |
| A2-66 | `apps/api/src/pages/gold-path.ts:100,107,109,112` | 「Proposal 打开已进入审计」「AI assistant 等你审批…」 | B | 中 | 「这次变更申请已记入审计」「Cuu 等你审批《…》」 | 中文串混未翻译实体名；`productCopy()`（第 14 行）把 cuu 机械替成英文 `AI assistant` |
| A2-67 | `packages/agent/src/fixtures/gold-path.ts:927,932,937` | 「澄清以点选为主」「交付物变更必须逐条可审」「回放页脚必须显示成本明细」 | B | 中 | 换成业务口径的验收项 | 示例工作项的验收清单三条全是产品自己的设计验收标准（id 就叫 `option-first`/`non-code-pr`/`replay-cost`），经 `/api/pages/gold-path` 对外提供 |

`apps/api/src/pages/i18n.ts` 全篇干净，无发现。

---

## 四、错误与审批 / 升级 / 通知文案

| # | 位置 | 原文 | 严重度 | 建议 |
|---|---|---|---|---|
| A2-68 | `apps/api/src/services/project-planner.ts:811` | 「物化时检出依赖成环（**judge 漏网**），已整体回滚，请重新规划。」 | 高 | 「这份计划里的任务互相依赖成了死循环，没有生成任何任务。请调整依赖后重新生成计划。」 |
| A2-70 | `apps/api/src/workers/agent-runner.ts:2322` | 「AI 预算已被**并发在飞执行**占满，本次未启动。」 | 高 | 「正在跑的任务已经占满了预算，这次没能开始。等其中一个跑完，或者先调高预算。」 |
| A2-71 | `apps/api/src/workers/agent-runner.ts:2457` | 「AI 执行**租约**已恢复，正在重新排队。」 | 中 | 「这次执行已恢复，正在重新排队。」 |
| A2-72 | `apps/api/src/workers/agent-runner.ts:1145,1146` | 「子任务**心跳超时**且多次恢复失败…」 | 中 | 「这个子任务卡住了，自动恢复也没成功。到决策收件箱里选：重试、转成我来做，或取消。」 |
| A2-73 | `apps/api/src/workers/agent-runner.ts:2312,2317` | 「AI **预算预留服务**暂时不可用」 | 中 | 「预算暂时算不出来，这次没有启动，请稍后再试。」 |
| A2-74 | `apps/api/src/services/human-reserved-guard.ts:143,167,174` | 「**我**不会让 AI 工人自动施工」「**我已经停止自动工具调用**」「**用户**明确不让…」 | 中 | 「「X」被标记为只能人来做，已经转给负责人接手。」等 |
| A2-75 | `apps/api/src/services/proposals.ts:1110,1143,1168` | 「不是 **UTF-8** 文本」 | 中 | 「不是纯文本，没法逐段合并」 |
| A2-76 | `apps/api/src/services/proposals.ts:1135,1143,1151` | 「文本**三方合并**的 **base 版本**」 | 中 | 「改动前的原始版本」 |
| A2-77 | `apps/api/src/services/proposals.ts:981` | 「没有通过字段补丁 **dry-run**」 | 中 | 「没通过写入前的检查」（`packages/ui/src/i18n.ts:152` 已有「试运行检查」） |
| A2-78 | `apps/api/src/services/proposals.ts:2000` | 「变更申请**分支**不属于这个事项」 | 中 | 「这份变更申请不属于这个任务」 |
| A2-79 | `apps/api/src/services/proposals.ts:1832`（+742/749/853/869/877） | 「没有找到这个合并建议：`<uuid>`」「字段 `acceptance_criteria` 的编辑…」 | 中 | 去掉 uuid；字段名走 `packages/ui/src/structured-field-labels.ts` 的人话标签 |
| A2-80 | `apps/api/src/services/approvals.ts:936` | `decided_by_label = userId.slice(0,8)` | 中 | 用昵称，取不到用「其他成员」 |
| A2-81 | `apps/api/src/services/escalations.ts:419` | 「已用 12000/20000 **令牌**…剩余 8000 令牌」 | 中 | 「已用掉 60% 的预算（¥0.42 / ¥1.00），还剩 ¥0.58。」——「令牌」在本产品里已被邀请码占用，一词两义 |
| A2-82 | `apps/api/src/services/cross-agent-judge.ts:287`（+`agent-runner.ts` 5 处 `"WorkHub AI"`) | `label: "WorkHub AI review"` | 中 | 「AI 复核」（`apps/api/src/pages/i18n.ts:73` 已有双语标签未复用） |
| A2-83 | `apps/api/src/services/cross-agent-judge.ts:378` | 「采用输出：`<候选 id>`」 | 中 | 用候选的人话名称 |
| A2-84 | `packages/web-runtime/src/notice.ts:177` | 「…并在有 **Proposal** 或 **Replay** 时提醒你。」 | 中 | 「做出改动或有回放时会提醒你。」 |
| A2-85 | `apps/api/src/services/task-plans.ts:206` | 「每个子任务都有验收标准，**依赖无环**，预算份额合计 100。」 | 中 | 「…任务之间没有互相等待的死循环，预算加起来正好 100%。」 |
| A2-86 | `apps/api/src/services/work-items.ts:296,297` | 「**PR 式**变更说明」「**第一阶段**只改文件」 | 中 | 去掉「PR 式」与阶段编号 |
| A2-69 | `apps/api/src/services/project-planner.ts:363` | 「AI 返回的项目计划格式无效：`${reason}`」 | 中 | 「这次没能生成可用的计划，请补充一下规划意图再试一次。」 |
| A2-87 | `apps/api/src/services/work-items.ts:292` | 「需要先确认一个关键点」 | 低 | 「这次交付要依据哪份材料？」 |

A2-87 值得单独一提：这正是澄清提示词里点名禁止的泛化标题（`apps/api/expected/clarification-prompt.zh.expected.md`：「不要使用"需要确认一个关键点"这类泛化标题」）——产品自己定的规矩没有约束到兜底文案。

**系统性（A2-94）：** `apps/api/src/services/cross-agent-judge.ts:337-368` 的 `publicReviewCopy()` 与 `packages/ui/src/proposal/render.ts:134-144` 的 `normalizePublicProposalCopy()` 是两层**事后正则洗词**，把 `R9.4 cross-agent judge arbitration`、`judge_not_independent`、`Raw judge decision:`、`failed closed`、`Meta-Planner`、`派发` 等替换成中文。替换表里出现「R9.4」本身就证明轮次代号曾进过用户可见的审阅理由；而不在表里的串（候选 id，见 A2-83）一律原样出街。附带发现：`proposal/render.ts` 把「派发」洗成「开始执行」，但 `apps/web/src/browser.ts:2062-2085` 与 `proposal/render.ts:136-139` 仍在用「暂停派发/恢复派发」——同一个词两种处置。

---

## 五、交付物模板（技能合同 / 提示词规定的用户可见输出 / 落到界面的产出模板）

| # | 位置 | 原文 | 分类 | 严重度 | 建议 |
|---|---|---|---|---|---|
| A2-88 | `apps/api/src/services/proposals.ts:625-647` | `# AI 融合正式稿` / `- Proposal ID：<uuid>` / `- Merge Proposal ID：<uuid>` / `- 冲突目标：<key>` / `- 选择方案：<key>` / `- 候选来源：unknown` / `## 融合理由` / `## 融合内容` + JSON 围栏 | C | **高** | 只写合并后的正文本身；id、冲突 key、AI 理由全部留在变更申请页与回放里，不进文件。文件名兜底 `<change_id>.ai-fusion.md`（621 行）同改 |
| A2-89 | `apps/api/src/workers/agent-run-prompt.ts:52,55,62` | 「在总结中列为 **blocker**」「明确列出 **blockers**」「命令结果里出现 `[sandbox: … denied by policy]` …把它列为 blocker」 | C | 中 | 改为「在总结里写清『卡在哪、缺什么、建议谁来定』，用中文，不要用 blocker 这类英文词，也不要把 `[sandbox: …]` 抄进总结」 |
| A2-90 | `apps/api/src/workers/agent-run-prompt.ts:54` | 「三行人话总结，例如「**完成了：**X / 产出文件：a.md, b.csv / 未尽：Y」」 | C | 中 | 「「做出来的：X / 文件：… / 还差：Y」——写结果，不写『我完成了/我接下来会』这类自述」 |
| A2-91 | `packages/tools/skills/code-script/SKILL.md:13,14`；`data-analysis/SKILL.md:53` | 规定输出必须含「**沙箱内**未实测网络部分」；「在总结列为 **blocker**」 | C | 中 | 「网络部分未实际验证」；用中文描述卡点 |
| A2-92 | `packages/tools/skills/markdown-report/SKILL.md:25-26` | `## 未尽事项与建议` + `<没做的、做不了的（缺什么）、建议谁来定>` | C | 中 | `## 待确认与下一步` + `<还需要谁确认什么、缺哪份材料、建议的下一步>` |
| A2-93 | `packages/ui/src/gold-path/route-components.ts:4470` | 直渲 `skill.provenance.rationale_md`（AI 精修补丁时写给自己的理由） | C | 中 | 不渲染理由原文，只留「已精修 · 改了 N 处」 |

A2-88 是本轮最重的一条：它是采纳「AI 融合稿」后**真正落进项目网盘、用户会打开的交付文件**。它当前不是交付物，而是一份合并过程记录——标题自称「AI 融合正式稿」，前四行是内部 id 与枚举 key（候选来源还会写成 `unknown`），接着是 AI 自己的融合理由，最后把内容塞进 JSON 代码块。同时违反「交付物是自包含的最终态」与「不把 agent 的推理写进交付物」。

A2-90 有一个自相矛盾之处值得指挥者注意：提示词模板首词就是「完成了：」，而下游 `packages/ui/src/proposal/render.ts:158` 的 `isModelSelfNarration()` 正则专门在过滤「我已经/接下来我/完成了…让我来总结」这类自述——源头模板与下游过滤器在互相打架，应在源头改。

---

## 覆盖清单

**逐文件读完（非抽样）：**
- `apps/web/src/**` 全部非测试文件（`routes.ts` 1900 行、`browser.ts` 6500 行的全部中文/英文字符串字面量提取后逐条过；另 `auth-screen-mode.ts` `objective-panel.ts` `drive-preview.ts` `my-conversations.ts` `project-lifecycle.ts` `settings-devices.ts` `workitem-collaboration.ts` `workspace-audit.ts` `avatar-crop-modal.ts` `ai-readiness-banner.ts` `delegate-options.ts` `react-route-mount.ts`）
- `packages/ui/src/**`：`gold-path/i18n.ts`（全文逐行）、`gold-path/route-components.ts` 6298 行（字典段 607-1200 逐行；其余按字符串字面量全量提取 740 条去重后逐条过）、`gold-path/product-shell.ts`（全部文案段 + 渲染点）、`gold-path/render.ts`、`gold-path/app-shell.ts`、`route-state.ts`（全文）、`i18n.ts`（全文）、`proposal/render.ts`、`replay/render.ts`、`workitem/render.ts`、`intake/render.ts`、`agent-run/render.ts`、`onboarding.ts`、`invite-accept.ts`、`structured-field-labels.ts`、`structured-field-details.ts`、`subrecord-item-diff.ts`、`rich-patch-viewer.ts`、`overlap-hunk-review.ts`、`route-line-editor.ts`、`avatar/*`
- `packages/web-runtime/src/**`（`notice.ts` `locale.ts` `action-payload.ts` 等）、`packages/contracts/src/locale.ts`
- `apps/api/src/pages/**` 全 10 个文件；`apps/api/src/http-error-codes.ts` 及其到界面的映射链（`packages/web-runtime/src/notice.ts:95-140`、`apps/web/src/browser.ts:2168/3761-3782/4291`、`packages/ui/src/route-state.ts:229`）
- `apps/api/src/services/`：`human-reserved-guard.ts` `approvals.ts` `escalations.ts` `notifications.ts` `meeting-analysis.ts` `task-plans.ts` `task-plan-approval.ts` `proposals.ts` `work-items.ts` `skill-curation.ts` `cross-agent-judge.ts` `project-planner.ts`；另 `apps/api/src/workers/agent-runner.ts` `agent-run-prompt.ts`
- `packages/tools/src/skills.ts` + `packages/tools/skills/*/SKILL.md` 七个预设技能全文
- `packages/agent/src/{observer,turns,reply-judge,spotlight-intent}/prompt.ts`、`packages/agent/src/fixtures/gold-path.ts` 相关段
- `apps/api/expected/*.md` 中规定用户可见输出形状的四份（clarification zh/en、meeting-analysis zh、agent-run system prompt）

**确认无用户可见文案（无发现）：** `packages/contracts/src/locale.ts`、`packages/web-runtime/src/locale.ts`、`apps/api/src/pages/i18n.ts`、`apps/api/src/http-error-codes.ts`（只产码，人话在前端）。

**确认不存在：** 邮件/推送模板——邀请走 out-of-band 链接（`apps/api/src/routes/auth.ts:727`），全仓无 SMTP/nodemailer/web-push。

**未纳入（不在本席位范围）：** `apps/desktop-webview/**`、`client-tauri/**`、`packages/cuu/src/i18n.ts`、测试与 `expected/` 断言本身、`apps/*/qa/**`。

---

## 不算违规但值得斟酌

1. **颜文字**（`(=^･ω･^=)`、`٩(◜◡◝)۶`、`(´；ω；`)`）遍布 gold-path 词典与首页空态。这是 R7 定的产品声音，不是违规；但它只出现在 zh/en 的部分 key 上（如 `home.emptyTitle` 有、`home.entryText` 没有），冷热不均像遗漏而非风格。建议要么全量铺，要么收敛到「Cuu 说话」的场景。
2. **Cuu 第一人称**（「先点一个选项，我再继续」`packages/ui/src/i18n.ts:26`、「我比较有把握/我拿不准，你来定」`route-components.ts:2940`、「今天我搞定了」`route-components.ts:1622`）。角色化第一人称是产品设定，本身成立；问题是同一批 key 的 en 侧改成了第三人称（`AI has started`、`AI handled today:`），中英人格不一致。建议定一条：Cuu 气泡与澄清用第一人称，系统提示与状态标签用第三人称，两语同策。
3. **`packages/ui/src/gold-path/i18n.ts:425`「此动作暂未支持。」** 诚实优于假装成功，保留；但配合 `runtime.notice.pendingTitle`「暂不可用」出现时，用户不知道是「永远没有」还是「稍后重试」。建议区分「这个入口在桌面客户端」与「这个动作不存在」两种说法。
4. **`route-components.ts:4462` 技能「成熟度 82%」**。glossary §1.2 第 3 条要求用户面零裸数值阈值；这里带了解释性 tooltip，属边界内。若要更稳，可改为「可放心复用 / 建议先看一眼 / 还在观察」三档。
5. **`apps/api/expected/meeting-analysis-system-prompt.zh.expected.md` 规则 1**：「转写太薄时仍要写 minutes_md 说明这一点」——会让纪要交付物里出现一句关于自己无法总结的话。比编造结论好，保留；建议在提示词里钉死措辞为业务口径（「这场会议的转写内容过少，没有可整理的结论。」），而不是让模型自由发挥成自述。
6. **`packages/ui/src/route-state.ts:230` 错误卡底部的 `status=… code=…`**（来自 `apps/web/src/browser.ts:2168`）。正文已有人话解释（「已记录出错信息，你可以重试…」），按规则不算违规；但建议把它做成可折叠的「技术详情」，而不是常驻一行。
7. **`route-components.ts:6160`「系统诊断（管理员关注；普通成员只读参考）」** 是唯一明确标出的开发者面板，其内容（A2-45 的工作进程数/消息服务/数据库）应当全部收进这里而不是散在设置主区——这是修复 A2-45 的落点。
8. **两处正向范例，建议作为修复基线复用：** `packages/agent/src/observer/prompt.ts:69`「title_md 必须是用户能直接看懂的人话，不出现 branch/PR/merge/commit 等技术黑话」；`apps/api/src/services/cross-agent-judge.ts:272`「Write every string in reasons and summary_md in Chinese, in plain product language a business user can read; do not use internal jargon or English status words」。所有会产出用户可见文本的提示词都应带上同款条款。
9. **词表统一是先决条件。** 同一实体四个叫法：任务(95/20/90) · 工作项(45/23/86) · 事项(25/26/80) · 工单(8/5/31)（计数=ui/web/api 非测试文件）；快照(25/7/32) vs 还原点(1/0/0)；自主率 vs 自治率；军团 vs 智能代理军团 vs 指挥台。`docs/workhub/00-overview/glossary-dejargon.md` 已是权威词表且 `docs/workhub/07-open-questions.md:319` 记录过一次「R9-OQ-NAMING-PASS」并标为已解决——实际未收敛。建议本轮修复不要逐条改词，而是先据 glossary 出一份 key→权威词映射，再机械替换 + 同步断言。

---

## 禁词门缺口（对照 `scripts/dev/check-copy-terms.ts`）

**当前门禁：** 5 个词典文件 × 10 条禁词规则，且只检查含中文字符的行。

### 缺口 1（最关键）：英文文案完全不检
第 60 行 `if (!/[一-鿿]/.test(line)) return;` 让 gold-path 词典的 en 段整块豁免。实证：`option-first` 已在禁词表里，却在**被扫描的文件内**存活两处（`gold-path/i18n.ts:644`、`route-components.ts:923`）。**修法：** 去掉中文行过滤，改为对字符串字面量取词；或对 en 段单独跑一份英文禁词表。

### 缺口 2：文件覆盖面
门禁只扫 `gold-path/i18n.ts`、`gold-path/route-components.ts`、`ui/src/i18n.ts`、`api/src/pages/i18n.ts`、`cuu/src/i18n.ts`。本轮 96 条里有 41 条落在**未被扫描**的文件：
`packages/ui/src/route-state.ts`、`packages/ui/src/gold-path/product-shell.ts`、`packages/ui/src/gold-path/render.ts`、`packages/ui/src/{proposal,replay,workitem}/render.ts`、`packages/ui/src/structured-field-details.ts`、`packages/ui/src/onboarding.ts`、`packages/ui/src/invite-accept.ts`、`packages/web-runtime/src/notice.ts`、`apps/web/src/{browser,routes,auth-screen-mode,objective-panel,drive-preview}.ts`、`apps/api/src/pages/{cost,replay,gold-path}.ts`、`apps/api/src/services/**`、`apps/api/src/workers/agent-runner.ts`、`packages/tools/skills/*/SKILL.md`。
「租约」「UTF-8」「快照 id」三条已在禁词表的规则，都在这些未扫文件里被违反（A2-71 / A2-75 / A2-48）。

### 缺口 3：审查纪律第 5 条的短语一条都没进表
缺：`我将`、`我们可以`、`本页面用于展示`、`这里会`、`用于说明`、`实现了`、`本页`、`此处展示`、`演示`、`占位`、`预览夹具`。
本轮命中：`本页`（`route-components.ts:3460/4781`）、`此页`（`4785`）、`这里会`（`gold-path/i18n.ts:426`、`route-components.ts:5396/3808`）、「页面只展示」（`gold-path/i18n.ts:399`）。

### 缺口 4：B 类（过程与理由泄漏）几乎没进表
建议新增（按本轮实际命中排序）：

| 类别 | 建议加入 | 本轮命中处 |
|---|---|---|
| 版本轮次 / 内部代号 | `R\d+(\.\d+)?`、`第 ?[0-9]+ ?轮`、`阶段 ?[0-9一二三四五六]`、`P0\.5`、`loop2`、`gold path`、`option-first`（扩到英文）、`DeepSeek Harness`、`Meta-?Planner`、`军团`、`试点`、`第 ?\d+ ?档` | A2-06 / A2-15 / A2-32 / A2-38 / A2-42 / A2-62 / A2-86 / A2-94 |
| 内部实现名词 | `物化`、`挂链`、`施工`、`回灌`、`路由`(动词)、`心跳`、`在飞`、`并发`、`实例`、`部署`、`后端`、`服务端`、`运行时`、`环境变量`、`哈希`、`失焦`、`注入`、`缓存`、`工作进程`、`成环`、`依赖无环`、`三方合并`、`base 版本`、`dry-?run`、`分支`、`PR 式`、`hunk`、`repo`、`payload`、`contract`、`fallback` | A2-08/23/28/45/54/55/56/57/59/60/68/70/72/73/74/76/77/78/85/86 |
| 裸枚举 / 裸 id | `"(ok\|warning\|critical\|exhausted\|accept_incoming\|keep_current\|ai_fusion\|field_merge\|escalated\|pm_mode)"` 作为完整字符串值；模板串里出现 `\.slice\(0, ?\d+\)` 且拼进用户文案 | A2-13 / A2-36 / A2-37 / A2-47 / A2-48 / A2-63 / A2-64 / A2-79 / A2-80 / A2-83 |
| 开发期真假数据口径 | `真实数据`、`假的?成功`、`Live data`、`实时数据`（作为徽章/区块标题）、`fake` | A2-05 / A2-07 / A2-33 |
| 非最终态 | `逐步支持`、`还没接`、`未接入`、`暂未实现`、`仍在`、`旧版`、`新版`、`改版` | A2-19 / A2-31 |
| 交付物 agent 自述 | 在 `packages/tools/skills/**` 与 `**/prompt.ts` 上单独跑一份规则：`blocker`、`沙箱`、`完成了：`、`我已经`、`接下来我` | A2-89 / A2-90 / A2-91 |

### 缺口 5：门禁没有覆盖「同一概念两套话」
所有词表类违规（任务/工作项/事项/工单、军团/智能代理军团/指挥台、快照/还原点、自主率/自治率、派发/开始执行、令牌 二义）都不是单词禁用能抓的。**建议：** 在 glossary 落一份 `canonical-terms.json`（权威词 + 禁用同义词列表），门禁改成「同一权威概念的非权威说法出现即报」，比继续往 BANNED 数组里堆正则更能收住这类问题。
