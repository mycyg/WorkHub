{
  "name": "@workhub/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node ../../scripts/dev/noop-test.mjs @workhub/web",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
