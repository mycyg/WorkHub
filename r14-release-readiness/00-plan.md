# R14 · 发布就绪（聊天完整度 + 全局搜索 + 记忆治理 + 病灶清扫 + 开源就绪）规划

> 状态：规划 · 2026-07-13 起草 · 上游：R13（Wave E2 施工中，收尾后先过双端全链终验再开 R14）
> 触发：发布级产品缺口分析后用户逐项拍板（见 §0）
> 定位修正：本项目**不商业化，内部使用 + 开源**。所有企业向条目（SSO/SCIM/合规/计费）永久出局；开源就绪本身升级为发布项。
> 纪律：本文档只规划不施工；施工沿用并行批次模式（r12-desktop-workbench/04 手册铁律不变，含无 emoji/去黑话/文档回真）。

---

## 0. 用户拍板记录（2026-07-13，逐字级结论）

| 条目 | 裁决 | 备注 |
|---|---|---|
| 聊天基础完整度 | **做** | 编辑/删除/引用回复/回应/已读/在线，见批 CHAT |
| 全局搜索 | **做** | 见批 SEARCH |
| 记忆的可见可治理 | **做** | 用户原话「可以诶」，见批 MEM |
| 优化空间清单 | **做** | 用户原话「你也搞吧」，见批 FIX / PERF |
| 移动端 | **做全功能，但放最后** | 用户明确嫌麻烦、要全功能不要缩水版；只留占位设计，排全部批次之后 |
| 打扰预算统一治理 | **不做** | 维持现状三套机制（回话判定限频/观察者静默/通知静音），不升维 |
| 数据主权/导出 | **不做** | 开源项目定位下用户明确不管 |
| 商业化配套 | **永久出局** | 不商业化；SSO/SCIM/审计合规/多租户计费等全部划掉 |
| 其余候选（GitHub 集成/反馈闭环/风险巡检/开源就绪/i18n/导览） | **授权我判断** | 我的取舍见 §1 批次表与 §4 非目标 |

---

## 1. 批次划分（依赖序即建议施工序）

| 批 | 名称 | 一句话范围 | 体量 | 依赖 |
|---|---|---|---|---|
| FIX | 病灶清扫 | 已立案未修缺陷收口 | 中 | R13 终验完成 |
| CHAT | 聊天基础完整度 | 编辑/删除/引用/回应/已读/在线 | 大 | FIX（同栈，先清病灶再动） |
| SEARCH | 全局搜索 | 跨会话/网盘/工单/会议统一检索 | 大 | 无（可与 CHAT 并行） |
| MEM | 记忆可见可治理 | 「Cuu 记住了什么」管理面 | 中 | 无（可并行） |
| FEEDBACK | AI 反馈闭环 | 回复/提议上的好评差评喂进夜间蒸馏 | 小中 | MEM 后顺路（同记忆栈） |
| RISK | 风险预警巡检 | PM 例行巡检：停滞/临期/成本异常主动汇报 | 小中 | 无（长在观察者+汇报既有底座上） |
| GH | GitHub 集成 | 项目绑仓，commit/PR 信号进 Cuu 感知 | 中 | 无；建议放 RISK 后（巡检有外部事实源才更值） |
| PERF | 长会话性能 | 消息列表虚拟化（桌面+web） | 中 | CHAT 后（DOM 结构稳定了再虚拟化） |
| OSS | 开源就绪 | LICENSE/README/部署指南/贡献指南/密钥卫生 | 小 | 随时可插队，开源时间点定优先级 |
| ONBOARD | 新手导览 | 「跟着 Cuu 做完第一个任务」引导线 | 小中 | 主功能面稳定后（倒数第二） |
| MOBILE | 全功能移动端 | 占位：技术路线设计先行，不缩水 | 特大 | **全部之后，最后一个** |

---

## 2. 各批范围草案（设计先行批施工时再出实现级设计稿）

### 批 FIX · 病灶清扫（第一优先）

已立案清单逐条收口，全部有既有 backlog 出处：

1. **P1-11**：turn 进行中发第二条消息被晾住——排队或友好提示，不再静默吞。
2. **escalated 死状态（GAP-2）**：升级流真正把 work_items 置 escalated，决策队列升级卡接上（此前枚举从没被设过）。
3. **drive 回滚两端不对称**：web/桌面回滚能力与文案对齐。
4. **通知深链缺 conversation_id**：通知契约补字段，气泡/通知点开直达会话（R12 备忘的 A1 项）。
5. **replay 组件化**：R10 余项，replay 渲染收编进组件体系。
6. **web smoke 偶发 /settings→onboarding flake**：查根因收掉，不再靠 rerun。
7. R13 终验中新捞出的缺陷插队进本批。

### 批 CHAT · 聊天基础完整度

数据模型（均为 additive 迁移）：
- `conversation_messages` 加 `edited_at`、`deleted_at`（墓碑：删除后正文清空、渲染「此消息已删除」占位，不物理删，审计链保留）、`reply_to_message_id`（引用回复，自引/跨会话禁止）。
- 新表 `message_reactions(message_id, user_id, reaction_key)` 唯一约束三元组；reaction 是**精选固定小集合**（赞成/反对/完成/疑问/关注 五个左右），渲染用 SVG 字符 tile——遵守「无 emoji」纪律（待拍板 §3-1）。
- 已读：新表或复用现有游标（施工时盘点 `conversation_participants` 是否已有 last_read_seq 类字段），群内显示「已读 N/M」聚合，不做逐人列表（隐私+噪音）。
- 在线状态（presence）：不落库，SSE 心跳内存态 + 定时衰减；头像点缀两态（在线/离线），typing indicator 已有不动。

规则红线：
- 编辑/删除只限本人消息、限时窗（建议 15 分钟，待拍板 §3-2）；Cuu 消息不可被人编辑；被引用的消息删除后引用侧显示墓碑。
- 编辑过的消息渲染「已编辑」标记；行动卡/工单等已从旧文本派生的下游**不回溯重算**（如实标注语义：派生物基于当时的原文）。
- 所有新事件走既有 SSE 契约扩展（照 zod 静默丢弃模式，旧客户端不炸）。
- P1-11 在 FIX 批修完后，本批的乐观 UI/失败重试补齐两端。

### 批 SEARCH · 全局搜索

- MVP 路线：**PG 原生，零新部署依赖**（开源自托管友好是硬约束）——`pg_trgm` GIN 索引做 CJK 子串匹配，覆盖：会话消息、网盘文档名+正文、工单标题+描述、会议纪要。不引 zhparser/pg_jieba（自托管装扩展的门槛劝退贡献者），不引外部搜索引擎。
- 统一端点 `GET /api/search?q=&scopes=`：逐 scope 各自 limit + 鉴权在 SQL 内逐 actor 收口（复用项目健康 DF-2 的教训，不做全局 cap 饿死）。
- 入口：聚焦盒新增「搜索全部」能力内联（现有意图分类扩一类）+ web 顶栏搜索页；结果分组渲染、点击直达（会话定位到消息 seq、网盘定位到文件、工单直达详情）。
- 语义检索（embedding）作为二期，等 MVP 用出真实痛点再立项，不在本批打包。

### 批 MEM · 记忆可见可治理

- 服务端：用户记忆 + 团队技能已有存储与夜间 curation，本批补**读写管理面**：列表/详情/编辑/删除/停用端点（PATCH 走受限编辑补丁模式，沿 K2 已有纪律）；每条记忆带出处（哪次会话/哪个 run 学到的，存储若缺出处字段则补 additive 迁移）。
- 前端：web 设置区「Cuu 的记忆」页 + 桌面 Spotlight 设置视图同款分区（落点在 R13 批 P3 的 AI 分区旁）；分「关于我」（用户记忆）与「团队技能」两 tab；删除需确认、显示「删除后 Cuu 将忘记这条」。
- 治理边界：团队技能的编辑权限=管理员，普通成员只读（待拍板 §3-3）。

### 批 FEEDBACK · AI 反馈闭环

- Cuu 文本回复、提议卡、行动卡上加「有用/没用」轻量反馈（字符 tile，不是 emoji）；落 `ai_feedback` 表（对象类型+对象 id+用户+判定+可选一句话）。
- 夜间 curation 消费：差评样本进技能蒸馏的反例池，好评强化；成本记账沿 K5 分账。
- 不做打分制/五星——二值+可选备注，降低反馈摩擦。

### 批 RISK · 风险预警巡检

- 新 worker：定时巡检（复用 scheduler 范式 + isConfigured 守卫），PM 视角三类信号：工单停滞 N 天、deadline 临近未动工、项目成本异常放量。
- 产出走既有主动汇报通道（会话内 Cuu 汇报消息 + 通知），限频合并（一天一次 digest 式，不逐条骚扰——这是批内固定策略，不是打扰预算升维）。
- 全部阈值进 settings 可配，默认保守。

### 批 GH · GitHub 集成（我的判断：做，理由=内部研发团队+开源社区双受益，Cuu 从自报进度升级为客观事实感知）

- 项目级绑定 repo（PAT/deploy key，服务端存储加密，桌面端零密钥纪律不变）；轮询为主（自托管无公网 webhook 也能用），webhook 可选加速。
- 信号消费：commit/PR/issue 动态进项目健康与 Cuu 巡检上下文；军团 run 产出若关联 PR，链接进提议卡。
- 明确不做：代码托管镜像、CI 状态面板、review 工具——只做「信号进感知」，不重造 GitHub。

### 批 PERF · 长会话性能

- 桌面工作台 + web 会话页消息列表虚拟化（自写窗口化渲染，不引重依赖）；滚动锚定（新消息在底部时跟随、用户上翻时不跳）。
- 上下文压缩（R13 批 C1）解决了 AI 侧，本批解决人侧 DOM。
- 图片/文件卡懒加载顺路。

### 批 OSS · 开源就绪

- LICENSE 拍板（待拍板 §3-4：MIT/Apache-2.0/AGPL 取向）；README 重写为开源门面（英文为主中文并存，快速自托管三步走）；CONTRIBUTING + 架构图；`.env.example` 全量核对；git 历史与代码密钥扫描（真 key 曾在本地 env 出没，开源前必扫）；docker-compose 自托管指南从 pilot 升级为正式路径；demo seed 一键出可玩环境。
- 插队时机由用户定开源时间点。

### 批 ONBOARD · 新手导览

- 「跟着 Cuu 做完第一个任务」：首次进工作台由 Cuu 在主区会话里引导（发消息→拎活→行动卡→批准→看产出），用真机制跑通而非假教程浮层；可跳过、可重看。
- 空态文案全面复查一遍（沿 R8「一无所知用户」审查纪律）。

### 批 MOBILE · 全功能移动端（最后，本轮只留占位）

- 用户要求全功能非缩水版。技术路线届时设计先行单独出稿，候选：Tauri 2 mobile（复用现有 webview 资产最大）vs 响应式 web 直接适配 vs 原生。倾向 Tauri mobile + 现有 desktop-webview 组件体系做自适应布局，但**不在本文档定死**——排期到 R14 全部批次之后，届时另出 00-plan。

---

## 3. 待用户拍板（不阻塞 FIX/SEARCH/MEM 先行）

1. **reaction 视觉与集合**：无 emoji 纪律下用 SVG/字符 tile 画精选五个（赞成/反对/完成/疑问/关注）——集合与画法确认？还是破例允许 emoji reaction？
2. **编辑/删除时窗**：建议本人消息 15 分钟内可编辑、删除不限时（墓碑化）——认不认？
3. **团队技能编辑权**：管理员可编辑、成员只读——认不认？
4. **开源 LICENSE 与时间点**：MIT（最省心）/Apache-2.0（专利条款）/AGPL（防闭源搭车）；开源在 R14 哪个节点执行，决定 OSS 批插队位置。
5. **GH 批去留**：我判断做（理由见批内），用户一票否决权在此。

## 4. 非目标（本轮明确不做，防 scope 漂移）

- 打扰预算统一治理（用户否决，维持三套现状机制）。
- 数据导出/数据主权面板（用户放弃）。
- SSO/SCIM/合规审计/计费等一切商业化配套（定位不商业化）。
- i18n 抽取：内部中文团队 + 开源后 UI 仍中文优先，只在 OSS 批把 README/部署文档做英文；UI 文案抽取工程量大收益低，明确不做（开源社区有需求再议）。
- 语义检索/embedding（SEARCH 二期再议）。
- 外部 IM 桥接（Slack/飞书/钉钉）——内部用定位下没有真实需求方。

## 5. 验收与纪律

- 每批沿用：范围围栏 + 04 手册 13 条铁律 + 全量门（各包测试+typecheck）+ 真机/真库验证 + 汇报落 reports/。
- R14 开工前置条件：R13 Wave E2 合并收口 + 双端全链终验（人到人/Cuu 到人数据打通矩阵）通过。
- 迁移号从 R13 实际用到的下一号起顺排，施工时由集成者统一分配。
