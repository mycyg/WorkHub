# WorkHub pilot 环境配置模板。复制为 .env.pilot 后按注释填写。
# 用法：cp .env.pilot.example .env.pilot && 编辑 → docker compose -f docker-compose.pilot.yml up -d --build

# ===== 必填（安全） =====
# 会话 cookie 签名密钥：生产必须是强随机串（生成：openssl rand -hex 32）。
COOKIE_SECRET=please-generate-a-strong-random-secret
# 管理员口令：第一个用户在注册屏展开"我是管理员"并填它来认领 admin。
ADMIN_CLAIM_SECRET=please-set-an-admin-passphrase

# ===== 必填（基础） =====
# 浏览器访问地址不是 https 时保持 false（LAN 内常见）。
COOKIE_SECURE=false
# LAN 内允许的来源；用具体地址替换（如 http://192.168.1.10:8787）。
CORS_ALLOW_ORIGINS=*

# ===== 数据库口令（与 compose 内 postgres 服务联动） =====
POSTGRES_PASSWORD=workhub

# ===== AI 工人（R5.10 真 key 验证时填） =====
# DeepSeek（Anthropic 协议端点）默认配置已内置，只需填 key：
LLM_API_KEY=
# 或自定义端点/模型：
# LLM_BASE_URL=https://api.deepseek.com/anthropic
# LLM_MODEL=deepseek-v4-flash
# LLM_MAX_TOKENS_PER_STEP=8192
# run_command（让 AI 跑 python/node 产出 Word/Excel/PPT/图表）默认 fail-closed（关）。
# 本机 LAN 单机试运行是受信环境，按需打开；多租户/公网部署务必保持 false 并改用隔离 runner。
AGENT_RUN_ALLOW_UNSANDBOXED_COMMANDS=true
# 团队技能夜间自学（见 DEPLOY.md §3.4）：队列闲时 AI 回看已完成的工作（含成员的差评）蒸馏可复用技能。
# 默认开启；没填上面的 LLM_API_KEY 时不会启动，每轮另受当日蒸馏花费上限约束。想关掉才改成 false。
# AGENT_RUN_SKILL_CURATION_ENABLED=true

# ===== GitHub 集成（可选，见 DEPLOY.md §3.3） =====
# 项目绑仓库后 commit/PR/issue 动态进 Cuu 感知。留空则该功能保持关闭（绑定端点 fail-closed 503）。
# 生成：openssl rand -base64 32（必须解码为 32 字节）。
# GITHUB_TOKEN_ENC_KEY=
# 绑定仓库多少天没有新提交算「长期没动静」（进每日风险巡检；1–90，默认 7）。
# GITHUB_STALE_DAYS=7

# ===== 可选 =====
# 对外端口（默认 8787）。
# WORKHUB_PORT=8787
# 日志格式：json（默认，便于采集）或 pretty（人读）。
# LOG_FORMAT=json
# 多 worker（pilot 单机默认 1；调大前先读 r2-release-gate.md）。
# WORKER_COUNT=1
